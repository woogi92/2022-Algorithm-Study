#include <stdio.h>
#include <string.h>

int main() {
  char s[100];
  int alpha[26] = { 0 };
  scanf("%s", s);
  int len = strlen(s);
  for(int i = 0; i < len; i++) {
    char c = s[i];
  }
  return 1;
}